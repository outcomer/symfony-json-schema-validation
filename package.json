{
  "name": "outcomer-validation-docs",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs --host 0.0.0.0",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host 0.0.0.0",
    "docs:deploy": "npm run docs:build && mkdir -p ../../../public/symfony-json-schema-validation && cp -r docs/.vitepress/dist/* ../../../public/symfony-json-schema-validation/"
  },
  "devDependencies": {
    "medium-zoom": "^1.1.0",
    "vitepress": "^1.0.0"
  }
}
